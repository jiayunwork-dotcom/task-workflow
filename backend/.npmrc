# 淘宝镜像源（中国大陆网络环境推荐使用，可根据需要注释或修改）
registry=https://registry.npmmirror.com

# 可选：其他镜像源
# registry=https://registry.npmjs.org/

# 安装时遇到可选依赖错误不中断构建
optional=false

# 增加超时时间
fetch-timeout=600000

# 缓存配置
cache=/tmp/npm-cache
